[package]
name = "signal"
version = "0.3.0"
edition = "2024"
description = "Map short mood inputs to actionable one-line suggestions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"
